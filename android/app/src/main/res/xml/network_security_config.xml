<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext traffic for OAuth deep link fallback -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- OAuth deep link fallback - browser converts scheme:// to http:// -->
        <domain includeSubdomains="true">kr.bluesky.dodo</domain>
    </domain-config>

    <!-- Default: Allow HTTPS for all other domains (Google, Supabase, etc.) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>
</network-security-config>
